# 心率监测系统 - 完整使用手册
# STM32H7 + MAX30102 + ESP01 WiFi + Android APP

---

## 一、系统总览

```
┌─────────┐   I2C    ┌───────────┐  UART   ┌───────┐   WiFi   ┌──────┐
│ MAX30102 │◄────────►│  STM32H7  │◄───────►│ ESP01 │◄────────►│ 手机 │
│ 心率传感器│  PB7/PB6 │  主控制器  │ PA10/PA9│WiFi模块│  TCP    │  APP │
└─────────┘          └─────┬─────┘         └───────┘         └──────┘
                           │
                    ┌──────┼──────┐
                    │      │      │
                 OLED屏  按键   蜂鸣器
                (I2C)  (PA2)   (PA3)
```

**数据流向：**
1. MAX30102 → STM32（I2C采集心率原始数据）
2. STM32内部处理（计算心率、AI异常检测、评分、OLED显示）
3. STM32 → ESP01（UART发送极简数据：`心率,评分,状态\n`）
4. ESP01 → 手机APP（WiFi TCP转发，手机端处理95%计算）

---

## 二、硬件接线表

### MAX30102 心率传感器 → STM32H7

| MAX30102引脚 | STM32H7引脚 | 说明 |
|-------------|-----------|------|
| VCC | 3.3V | 供电 |
| GND | GND | 地 |
| SDA | PB7 | I2C数据 |
| SCL | PB6 | I2C时钟 |
| INT | 可选 | 中断引脚 |

### OLED显示屏 (SSD1306) → STM32H7

| OLED引脚 | STM32H7引脚 | 说明 |
|---------|-----------|------|
| VCC | 3.3V | 供电 |
| GND | GND | 地 |
| SDA | PB7 | 与MAX30102共用I2C |
| SCL | PB6 | 与MAX30102共用I2C |

### ESP01 WiFi模块 → STM32H7

| ESP01引脚 | STM32H7引脚 | 说明 |
|----------|-----------|------|
| VCC | 3.3V | ⚠️必须3.3V，不能用5V！ |
| GND | GND | 地 |
| TX | PA9 (Serial1 RX) | ESP01发送→STM32接收 |
| RX | PA10 (Serial1 TX) | STM32发送→ESP01接收 |
| EN (CH_PD) | 3.3V | 使能引脚，必须拉高 |
| RST | 3.3V (或接按钮) | 复位引脚 |

### 其他外设

| 外设 | STM32H7引脚 | 说明 |
|-----|-----------|------|
| 按键 | PA2 | INPUT_PULLUP，点按切换蜂鸣器 |
| 蜂鸣器 | PA3 | OUTPUT，LOW=响，HIGH=静 |

### ⚠️ ESP01 注意事项
1. **供电**：ESP01峰值电流可达300mA，建议加100μF电容
2. **电平**：STM32H7是3.3V电平，与ESP01兼容，无需电平转换
3. **波特率**：出厂默认115200，烧录固件后也是115200

---

## 三、ESP01 固件烧录指南

### 步骤1：准备烧录工具
- USB转TTL模块（如CH340、CP2102）
- 杜邦线若干

### 步骤2：ESP01进入烧录模式接线
```
USB转TTL      ESP01
─────────    ──────
TX       →   RX
RX       ←   TX
VCC(3.3V)→   VCC
GND      →   GND
         →   EN (接3.3V)
         →   GPIO0 (接GND) ← 烧录模式关键！
```

### 步骤3：烧录固件
1. 安装 [Arduino IDE](https://www.arduino.cc/en/software)
2. 安装ESP8266开发板支持：
   - 文件 → 首选项 → 附加开发板管理器URL
   - 添加：`http://arduino.esp8266.com/stable/package_esp8266com_index.json`
   - 工具 → 开发板 → 开发板管理器 → 搜索 "ESP8266" → 安装
3. 选择开发板：**"Generic ESP8266 Module"**
4. 打开 `HeartRateApp/esp01_firmware.ino`
5. 选择对应COM口
6. 点击"上传"按钮烧录

### 步骤4：烧录完成后
- 断开 GPIO0 与 GND 的连接
- 按ESP01的RST复位按钮（或重新上电）
- ESP01板载LED快闪3次表示启动成功

---

## 四、手机APP安装指南

### 📱 方案A：直接使用APK安装（推荐）

如果你已有APK文件：

1. 将APK文件通过**微信/U盘/数据线**传输到手机
2. 手机上打开文件管理器，找到APK文件
3. 点击安装
4. 如果提示"未知来源"，进入设置 → 安全 → 允许安装未知来源应用
5. 安装完成后打开APP

### 📦 方案B：自己打包APK

**方法1：GitHub自动打包（最简单）**
1. 注册GitHub账号（github.com）
2. 将整个 `HeartRateApp` 文件夹上传到GitHub仓库
3. 确保 `.github/workflows/build.yml` 文件存在
4. 进入仓库 → Actions → 运行 "Build Heart Rate Monitor APK"
5. 等待约10-15分钟
6. 在Artifacts区域下载生成的APK文件
7. 通过微信/U盘传输到手机安装

**方法2：Docker打包**
```bash
# 在电脑上安装Docker Desktop
# 打开命令行执行：
docker pull kivy/buildozer
docker run --rm -v "HeartRateApp文件夹完整路径:/app" kivy/buildozer android debug
# 生成的APK在 HeartRateApp/bin/ 目录下
```

**方法3：WSL打包（Windows用户）**
```bash
# 1. 安装WSL
wsl --install -d Ubuntu

# 2. 在WSL中执行
sudo apt update
sudo apt install -y git zip unzip openjdk-17-jdk python3-pip
pip3 install buildozer cython

# 3. 复制项目到WSL
# 在Windows资源管理器地址栏输入：\\wsl$\Ubuntu\home\
# 将HeartRateApp文件夹复制进去

# 4. 打包
cd ~/HeartRateApp
buildozer android debug
# APK在 bin/ 目录下
```

### 💻 方案C：电脑上测试运行（无需APK）
```bash
# 安装Python 3.8+
# 安装Kivy
pip install kivy

# 运行APP
cd HeartRateApp
python main.py
```

---

## 五、使用流程（一步步操作）

### 第1步：给STM32上电
1. 确保所有接线正确
2. 给STM32上电
3. OLED显示初始化信息
4. ESP01初始化（约5-8秒），WiFi热点 "HeartRateMonitor" 创建成功
5. OLED右上角显示 "WiFi●" 表示ESP01就绪

### 第2步：手机连接WiFi
1. 打开手机WiFi设置
2. 找到 **"HeartRateMonitor"** 网络
3. 输入密码：**12345678**
4. 等待连接成功（手机会提示"无法上网"，这是正常的，点"仍要连接"）

### 第3步：打开APP并连接
1. 打开心率监测APP
2. 点击 **"连接设备"** 按钮
3. APP自动连接到ESP01（192.168.4.1:8080）
4. 连接成功后状态显示 "已连接"（绿色）

### 第4步：开始监测
1. 将手指轻放在MAX30102传感器上
2. 点击APP的 **"开始监测"** 按钮
3. 观察数据实时更新：
   - **实时心率数值**：大字号红色数字（BPM）
   - **实时时间**：顶部显示当前时间
   - **评分**：基于异常概率计算（100分制）
   - **波形图**：最近60个心率数据点的曲线
   - **状态**：正常/注意/异常

### 第5步：查看检测总结
- APP底部自动显示检测总结：
  - 平均心率
  - 心率范围（最低-最高）
  - 平均评分
  - 健康评级
  - 异常比例
  - 检测时长
  - 健康建议

### 第6步：停止监测
1. 点击 **"停止监测"** 按钮
2. 查看最终检测总结
3. 点击 **"断开连接"** 关闭连接

---

## 六、APP界面说明

```
┌──────────────────────────┐
│   心率监测系统    已连接  │ ← 标题+连接状态
├──────────────────────────┤
│ ⏱ 14:35:28              │ ← 实时时间
├──────────────────────────┤
│                          │
│         78               │ ← 实时心率（大字号）
│        BPM               │
│                          │
├──────────────────────────┤
│ ⭐ 评分      95 分       │ ← 心率评分
│ ⚠ 状态      正常        │ ← 异常状态
├──────────────────────────┤
│ ～～波形图～～           │ ← 心率波形曲线
│ ～～～～～～～           │
├──────────────────────────┤
│ 📊 检测总结              │
│ ❤ 平均心率：75.3 BPM    │
│ 📉 心率范围：68-89 BPM   │
│ ⭐ 平均评分：92.5 分     │ ← 检测总结
│ 🏆 健康评级：良好        │
│ ⚠ 异常比例：5.2%        │
│ ⏱ 检测时长：3分25秒      │
├──────────────────────────┤
│ [连接设备]  [开始监测]   │ ← 控制按钮
└──────────────────────────┘
```

---

## 七、数据协议说明

STM32通过UART发送给ESP01的格式：
```
心率,评分,状态\n
```

| 字段 | 范围 | 说明 |
|-----|------|------|
| 心率 | 40-200 | 单位BPM |
| 评分 | 0-100 | 100 - 异常概率 |
| 状态 | 0/1/2 | 0=正常, 1=注意, 2=异常 |

**示例：**
```
78,95,0      → 心率78，评分95，正常
120,55,1     → 心率120，评分55，注意
150,20,2     → 心率150，评分20，异常
```

---

## 八、故障排查

| 问题 | 原因 | 解决方法 |
|-----|------|---------|
| OLED不亮 | I2C未接好 | 检查PB7/PB6接线 |
| WiFi显示○ | ESP01未就绪 | 检查ESP01接线和供电 |
| 手机搜不到WiFi | ESP01未启动 | 检查EN引脚是否接3.3V |
| 连接失败 | WiFi未连/端口错 | 确认手机连了HeartRateMonitor |
| 数据不更新 | 手指未放好 | 轻放手指在传感器上 |
| APP闪退 | Kivy兼容性 | 重新安装APP |
| 心率一直是0 | MAX30102未初始化 | 检查I2C地址0x57 |

---

## 九、文件清单（可通过微信/U盘传输）

```
HeartRateApp/
├── main.py               ← APP主程序（可电脑测试）
├── data_processor.py     ← 数据处理模块
├── heart_rate.kv         ← 界面定义
├── buildozer.spec        ← APK打包配置
├── requirements.txt      ← Python依赖
├── esp01_firmware.ino    ← ESP01固件代码
└── README.md             ← 本说明文档
```

**APK文件传输方式：**
- 微信：直接发送APK文件（可能需要改后缀为.zip，对方收到后改回.apk）
- U盘：直接复制APK文件
- 数据线：复制到手机存储
- QQ/钉钉：直接发送APK文件